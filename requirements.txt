rich>=13.7.0
python-dotenv>=1.0.0
prompt_toolkit>=3.0.0
pyfiglet>=0.8.0
fastmcp>=0.1.0
openai>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
duckduckgo-search>=7.0.0
plotly>=5.18.0
pandas>=2.0.0
graphviz>=0.20.0
kaleido>=0.2.1
nbformat>=5.0.0

# Optional: SAP AI Hub backend (requires SAP enterprise access).
# These packages are NOT required to run the agent — NVIDIA works without them.
# Uncomment the three lines below only if you have SAP AI Hub access:
sap-ai-sdk-gen>=6.6.0
boto3>=1.34.0
aiobotocore>=2.12.0
